//import React from "react";

const TypingNotification = ({ userName }) => {
  return (
    <div className="writing-notification">{`${userName} is writing...`}</div>
  );
};

export default TypingNotification;
