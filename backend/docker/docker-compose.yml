services:
  XatSprint7:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_DATABASE: 'XatSprint7'
      # So you don't have to use root, but you can if you like
      MYSQL_USER: 'XatSprint7'
      # You can use whatever password you like
      MYSQL_PASSWORD: 'XatSprint7'
      # Password for root access
      MYSQL_ROOT_PASSWORD: 'XatSprint7'
    ports:
      # <Port exposed> : < MySQL Port running inside container>
      - '3306:3306'
    expose:
      # Opens port 3306 on the container
      - '3306'
      # Where our data will be persisted
    volumes:
      - XatSprint7:/var/lib/mysql
# Names our volume
volumes:
  XatSprint7: